# 🖼️ Butler 图片 / UI 素材清单（观察.txt #5 #6 #14）

> 这份清单列出项目当前**已用** / **可补强** / **待你提供**的图片素材。
> 建议优先 **SVG**（可缩放、可跟随主题色 `currentColor` / CSS 变量），位图用透明底 PNG。
> 统一放 `my-app/apps/web/public/assets/`（复古主题素材放 `public/assets/retro/`）。
> 最近更新：2026-05-29

---

## 一、已具备（无需补）

| 素材 | 形式 | 位置 | 说明 |
|---|---|---|---|
| 管家 7 姿势立绘 | trim PNG | `public/assets/butler-*.png` | standing/serving/pointout/thinking/thinking-hard/idea/rare-thinking；暗色模式自动 invert 反白 |
| Logo | PNG | `public/assets/logo.png` | 顶栏品牌，暗色自动反白 |
| 6 空状态插画 | 内联 SVG | `components/EmptyIllustrations.tsx` | 任务/笔记/对话/搜索/筛选/面板；跟随主题色 |
| 成就 emoji | Unicode | `lib/streak.ts` | 8 成就用 emoji（🌱✅💪🚀📝🔥🌟👑），见下「可补强」 |

---

## 二、待你提供 —— 复古手绘主题（retro，**最高优先**，阻塞 task #11）

> 参考你给的 Notes 截图。CSS 素材槽变量已在 `globals.css` 的 `[data-theme="retro"]` 预留，交图即接入逼近 1:1。
> 放 `public/assets/retro/`，**SVG 优先**（线描墨色，透明底）。

| # | 素材 | 用途 / 位置 | 建议规格 | 接入变量 |
|---|---|---|---|---|
| 1 | **手绘墨线边框（九宫格）** | 各面板 / 卡片外框，可拉伸 | SVG，四角含交叉排线；标注九宫格 slice（如 32px） | `--retro-frame`（border-image）|
| 2 | **四角飘带 / 领结 ornament** | 贴面板四角的装饰角花 | SVG ×1（可旋转复用四角） | 绝对定位贴角 |
| 3 | **标题飘带横幅** | "PRIVATE WRITING DESK" / "BUTLER'S MEMOS" 那种缎带标题 | SVG，可放文字的中空横幅 | header 背景 |
| 4 | **城市天际线** | 左栏底部装饰（你截图左下角） | SVG / 透明 PNG，宽≈左栏 220px | `--retro-skyline` |
| 5 | **钢笔 + 墨水瓶** | 主区右下角装饰（你截图右下角） | SVG / 透明 PNG，≈200×200 | `--retro-deskware` |
| 6 | **管家端盘线描立绘** | Toast / 提示里的小管家（你截图右上角） | SVG / 透明 PNG，≈120px 高 | `--retro-butler` |
| 7（可选）| 半调网点 tile | 更精细的网点底（现已用 CSS 实现，够用）| 透明 PNG，无缝平铺 | 替换 `--bg-halftone` |

---

## 三、可补强 —— 提升精致度（非阻塞，有则更好）

| 素材 | 用途 | 形式 | 备注 |
|---|---|---|---|
| **8 枚成就徽章** | 成就收藏室（现用 emoji）| SVG | 自制金属/纸感徽章，区分 locked/unlocked |
| **OnboardingTour 5 步插画** | 新手引导每步配图 | SVG | 现为纯文字步骤 |
| **ShareCard 主题背景** | 分享卡片底图 | SVG / PNG | 现为纯色 |
| **季节 / 节日主题图** | 考试季 / 春节等氛围皮 | PNG | 锦上添花 |
| **管家专属元素图标** | 燕尾服 / 单框眼镜 / 白手套（观察 #21）| SVG | 做品牌点缀 icon，可入按钮 / 空态 |

---

## 四、AI 生成内容配图（观察.txt #14，待设计）

> AI 回复 / 任务卡里穿插的小图，让 AI 产出更直观。多为**按语义动态选用**的 icon/插画集：

| 类型 | 示例场景 | 形式 |
|---|---|---|
| 学科 icon | 数学 / 语言 / 编程 / 实验等任务分类 | SVG icon 集 |
| 进度 / 数据可视化 | 配合观察 #17 拖拽图表（饼 / 柱 / 热力）| 图表库渲染（非静态图）|
| 情绪 / 鼓励贴纸 | 完成 / 拖延 / 加油等管家反馈 | SVG 贴纸 |

---

## 五、交付建议

- **先给第二节（复古 6 件）的 1–2 个**（如城市天际线 + 钢笔墨水瓶），我先接出效果给你看对位，再补齐其余。
- 文件命名建议：`retro/frame.svg`、`retro/skyline.svg`、`retro/deskware.svg`、`retro/butler.svg`、`retro/corner.svg`、`retro/banner.svg`。
- 给图时附上**期望尺寸 / 是否透明底 / 九宫格 slice 值**（边框那张），我直接接。
